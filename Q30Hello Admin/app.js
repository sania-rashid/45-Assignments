"use strict";
let usernames = ["saqib", "aziz", "fahad", "waqar", "siraj"];
for (let i = 0; i < usernames.length; i++) {
    if (usernames[i] === 'saqib') {
        //console.log("Hello saqib, would you like to see a status report?");
    }
    else {
        //console.log(`Hello ${usernames[i]}, thank you for logging in again.`);
    }
}
